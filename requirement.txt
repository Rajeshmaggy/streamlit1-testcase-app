streamlit==1.25.0          # For building the web app
pandas==2.1.1              # For handling CSV files
opencv-python-headless==4.8.0.74  # For video processing
pytesseract==0.3.10        # For OCR
fpdf==1.7.2                # For generating PDF documents
openai==0.27.8             # For OpenAI GPT-based models (if applicable)
groq==0.3.5                # Groq client for LLaMA3 model (replace with the actual library name)
